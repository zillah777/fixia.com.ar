version: '3.8'

services:
  # Redis for rate limiting and caching
  redis:
    image: redis:7-alpine
    container_name: fixia-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    networks:
      - fixia-network

  # PostgreSQL database (optional - if not using external DB)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: fixia-postgres-dev
  #   environment:
  #     POSTGRES_USER: fixia
  #     POSTGRES_PASSWORD: fixia_dev_password
  #     POSTGRES_DB: fixia_dev
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U fixia"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 3
  #   restart: unless-stopped
  #   networks:
  #     - fixia-network

volumes:
  redis-data:
    driver: local
  # postgres-data:
  #   driver: local

networks:
  fixia-network:
    driver: bridge
